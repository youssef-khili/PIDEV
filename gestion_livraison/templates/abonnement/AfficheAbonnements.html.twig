{% extends 'student/base-back.html.twig' %}
    {% block body %}
<!DOCTYPE html>
<html>
<title>AffichageAbonnement</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<body>
  <h2>Liste des abonnements</h2>
  <p>Ceci est la liste des abonnements:</p>
  <button>trie
<a class="fas fa-sort"  href="{{ path('trie') }}">
</a>
  </button>
  <a class="btn btn-primary" href="{{path('imprimer_index')}}" > Imprimer </a>
  <table border="1">
      <tr class="w3-light-grey w3-hover-red">
        <th>id abonnement</th>
        <th>prix abonnement</th>
        <th>Date Debut abonnement</th>
        <th>Date Fin abonnement</th>
        <th>Remise abonnement</th>
        <th>Delete</th>
        <th>Update</th>
      </tr>
      <tr {% for a in abonnement%}>
        <td> {{ a.id }} </td>
        <td> {{ a.prixAbonnement }} </td>
        <td> {{ a.dateDebut.format('d/m/Y') }} </td>
        <td> {{ a.dateFin.format('d/m/Y') }} </td>
        <td> {{ a.Remise }} </td>
        <td> <a href="{{ path('d',{'id':a.id}) }}"> Delete </a></td>
        <td> <a href="{{ path('update',{'id':a.id}) }}"> Update </a></td>
      </tr {% endfor %}>
  </table>
  <ul>
        <li>Retour à <code><a href="{{ path('abonnement') }}">l'accueil des abonnements</a></code></li>
  </ul>
</body>
</html> 
{% endblock %}