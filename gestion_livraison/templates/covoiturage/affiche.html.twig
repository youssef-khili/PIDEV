{% extends "front.html.twig"  %}
{% block body%}
<!doctype html>
<html lang="en">
<head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <style type="text/css">
        table {margin: 0 auto;}
        table, tr, td {
            border: 2px #2b2b2b solid;
            color: #2b2b2b;
        }

        table { background-color: #D3D3D3; }
        tr { background-color: #FD6C9E; }
        a {
            color: Black;
        }

    </style>
</head>
<body>
{% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
{% endfor %}
<td>
        <a class="btn btn-success"href="{{ path('covoiturage_list') }}">pdf</a>
    </td>
<table >


    <tr>
        <th>Id_Covoiturage</th>
        <th>Depart</th>
        <th>Arrive</th>
        <th>Tarif</th>
        <th>Longueur_Trajet</th>
        <th>ID_Vehicule	</th>
        <th> Nombre_Passager</th>
        <th> Delete </th>
        <th>Update </Update>

    </tr>
    <tr {% for c in covoiturage %}>
        <td>{{ c.id_covoiturage}}  </td>
    <td>{{ c.depart}}  </td>
        <td>{{ c.arrive}}</td>
       <td>{{c.tarif}}</td>

        <td>{{c.longueurtrajet}}</td>
        <td>{{c.idveh}}</td>
        <td>{{c.nbpsg}}</td>
        <td><a href=" {{ path('d',{'id':c.id_covoiturage })}}">Delete</a> </td>
        <td><a href=" {{ path('update',{'id':c.id_covoiturage })}}">update</a> </td>
    </tr {% endfor %}>
</table>
</body>
</html>

{% endblock %}