{% extends 'backoffcie.html.twig' %}

{% block title %}Actualite{% endblock %}

{% block body %}
    <h1>Actualite</h1>

    <table class="table" border=""2>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actualite.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ actualite.titre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ actualite.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ actualite.date ? actualite.date|date('Y-m-d') : '' }}</td>
            </tr>
             <tr>
                <th>continu</th>
                <td>{{ actualite.continu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actualite_index') }}"> <button class="btn btn-primary">back to liste</button></a>

    <a href="{{ path('actualite_edit', {'id': actualite.id}) }}"> <button class="btn btn-primary">edit</button></a>

    {{ include('actualite/_delete_form.html.twig') }}
{% endblock %}
