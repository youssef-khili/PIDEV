{% extends 'front.html.twig' %}

{% block body %}

    <div class="example-wrapper">
        <h1>Reserver votre place  ✅</h1> <hr>

        <div class="row">
            <div class="col-md-6">
                {{ form_start(form) }}
                {{ form_row(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Reserver ici'}}) }}
                <hr>
                <button type="submit" class="btn btn-success btn-block">Reserver</button>
                {{ form_end(form) }}
            </div>
            <div class="col-md-6">
                {% if qrCode %}
                    <img src="{{ qrCode }}" alt="qr-code">
                {% endif %}
            </div>
        </div>
        <div class="masthead-heading text-uppercase">WELCOME TO WASSSALNI</div>
        <a class="btn btn-primary btn-xl text-uppercase" href="http://127.0.0.1:8000/Affichec">Retour</a>
    </div>
    </div>
{% endblock %}
