{% extends 'base.html.twig' %}

{% block title %}Reclamationn{% endblock %}

{% block body %}
    <h1>Reclamationn</h1>

    <table class="table"border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamationn.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamationn.date ? reclamationn.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ reclamationn.sujet }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ reclamationn.iduser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamationn_index') }}">back to list</a>

    <a href="{{ path('reclamationn_edit', {'id': reclamationn.id}) }}">edit</a>

    {{ include('reclamationn/_delete_form.html.twig') }}
{% endblock %}
