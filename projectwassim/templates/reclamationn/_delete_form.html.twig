<form method="post" action="{{ path('reclamationn_delete', {'id': reclamationn.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamationn.id) }}">
    <button class="btn">Delete</button>
</form>
