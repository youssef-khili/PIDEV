{% extends 'backoffice.html.twig' %}

{% block title %}Actualitee{% endblock %}

{% block body %}
    <h1>Actualitee</h1>

    <table class="table" border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actualitee.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ actualitee.titre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ actualitee.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('actualitee_index') }}">back to list</a>

    <a href="{{ path('actualitee_edit', {'id': actualitee.id}) }}">edit</a>

    {{ include('actualitee/_delete_form.html.twig') }}
{% endblock %}
